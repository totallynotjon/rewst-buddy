# Verify 001 — Raise the VS Code floor and add the chat-model contribution point

Task: 001-engines-and-provider-contribution
Status: VERIFIED

## What landed

- `package.json` `engines.vscode`: `^1.107.0` → `^1.122.0` (signed-out runtime floor, AC3).
- `package.json` `contributes.languageModelChatProviders`: added `[{vendor: rewst-buddy, displayName: RoboRewsty}]`.
- `package.json` devDeps `@types/vscode`: `^1.107.0` → `^1.120.0` (highest published; see decoupling note below). `package-lock.json` synced.
- `src/packageManifest.test.ts`: colocated unit test asserting the engines floor, the typings floor (>=1.120), and the contribution.

## Forced decoupling (validated by Codex gate)

The original plan pinned BOTH `engines.vscode` and `@types/vscode` at `^1.122.0`. `@types/vscode@^1.122.0` is **not published** on npm (highest is `1.120.0`; `npm install` → ETARGET). The `LanguageModelChatProvider` API finalized in VS Code 1.104, so the 1.120 typings already carry the full surface; the 1.122 change was runtime/licensing (no Copilot sign-in), not a typings symbol. Resolution: keep `engines.vscode ^1.122.0`, pin `@types/vscode ^1.120.0`. Recorded in `DECISIONS.md` → "Decoupling of @types/vscode from the engine floor". Codex validation gate `task-mq9th83b-44xym8` → **AGREE** (touched none).

## Done-check evidence (commands + exit codes)

1. Manifest assertion — engines `^1.122`, types `>=1.120`, contribution present:

    ```
    node -e "const p=require('./package.json'); if(!/\^1\.122/.test(p.engines.vscode)) throw 'engines'; if(!/\^1\.1(2[0-9]|[3-9])/.test(p.devDependencies['@types/vscode'])) throw 'types'; if(!p.contributes.languageModelChatProviders) throw 'contribution';"
    ```

    → MANIFEST EXIT 0

2. Type-check against the new `@types/vscode`:

    ```
    npm run type-check   # tsc --noEmit
    ```

    → TYPECHECK EXIT 0

3. Unit tests:
    ```
    npm run test:unit
    ```
    → 276 passing, 6 pending, extension host exit code 0, TESTUNIT EXIT 0
    → suite "Unit: package manifest" › "declares the VS Code chat model provider floor and contribution" ✔

## Acceptance-criterion linkage

- AC3 (signed-out floor): `engines.vscode ^1.122.0` asserted by unit test + grep. Manual signed-out acceptance check remains a documented release-checklist step (tracked for task 012 docs/checklist).
- AC1 (contribution point present): `contributes.languageModelChatProviders` declared (provider registration code is task 006).
