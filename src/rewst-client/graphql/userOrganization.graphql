query UserOrganization {
    userOrganization {
        createdAt
        domain
        id
        managingOrgId
        name
        orgSlug
        resultsRetentionDays
        rocSiteId
        tid
    }
}
