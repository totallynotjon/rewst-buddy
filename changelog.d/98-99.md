---
category: Fixed
---

- Fixed `buddy_workflow_executions` guidance for sub-workflow-only runs with `rootOnly:false`, and added `rewst-buddy.ai.maxBuddyToolRounds` so long Cage-Free Rewsty investigations can raise the in-process tool cap.
