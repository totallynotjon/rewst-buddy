---
category: Changed
---

- **GraphQL AI tools** - split safe schema/read access into always-available `buddy_graphql_schema` and `buddy_graphql_read` tools, and move raw GraphQL mutations behind the opt-in `graphqlUnsafe` setting as a last-resort, approval-gated tool.
