---
category: Added
---

**Section auto-layout** — workflow edits now re-arrange only the smallest one-line-in/one-line-out chunk around the change instead of the whole canvas, and `buddy_workflow_autolayout` accepts a `section` task for targeted tidying. Over-long transition lines are flagged after layout changes.
