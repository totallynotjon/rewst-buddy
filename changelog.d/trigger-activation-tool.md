---
category: Added
---

- **Safely edit trigger org activation** — the new `buddy_set_trigger_activation` MCP tool adds, removes, or replaces the orgs a trigger is activated for (and toggles auto-activate) without dropping untouched activations or clone overrides, creating a revertable patch and reporting a before/after diff.
