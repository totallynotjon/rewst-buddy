---
category: Added
---

- **Safely set trigger org activation** — the new `buddy_set_trigger_activation` MCP tool sets which orgs a trigger is activated for (and toggles auto-activate) without disturbing clone overrides, creating a revertable patch and reporting a before/after diff.
