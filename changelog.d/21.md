---
category: Added
pr: 21
---

- **Template sync over MCP** — Two tools let an AI assistant drive the link-and-sync workflow: `buddy_template_sync_status` reports a linked file's state (in-sync/conflict, read-only) and `buddy_template_sync` pushes or pulls it, stopping on a conflict instead of overwriting. Sync is a gated write tool.
