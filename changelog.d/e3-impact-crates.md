---
category: Added
---

**Impact and Crate discovery tools** — `buddy_workflow_impact` lists the workflows that would break before a sub-workflow's inputs/output change or a pack action upgrade, and `buddy_search_crates` finds prebuilt Rewst Crates (with per-org install status) so agents reuse instead of rebuilding.
