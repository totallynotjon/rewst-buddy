---
category: Fixed
---

- **Sub-workflow executions** — `buddy_workflow_executions` now surfaces runs of workflows that are only ever called as sub-workflows; pass `rootOnly:false` to search by workflow id across the invoking orgs.
