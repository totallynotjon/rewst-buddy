---
category: Added
pr: 68
---

- **MCP server (experimental): workflow write tools** — when write tools are enabled, external MCP clients can edit, auto-layout, and run Rewst workflows (`buddy_workflow_edit`, `buddy_workflow_autolayout`, `buddy_workflow_run`). Every change requires a per-workflow approval inside VS Code, so an external client can never approve its own write.
