---
category: Added
---

- Added read-only MCP tools to list a Rewst org's triggers, forms, tags, and trigger activation instances, plus a batch trigger health check (`buddy_get_trigger_error_status`).
