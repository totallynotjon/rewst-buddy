---
category: Added
pr: 80
---

- **MCP server (experimental): tag write tools** — when write tools are enabled, external MCP clients can create, update, and delete Rewst tags (`buddy_create_tag`, `buddy_update_tag`, `buddy_delete_tag`), each org-scoped and gated by a per-change approval inside VS Code.
