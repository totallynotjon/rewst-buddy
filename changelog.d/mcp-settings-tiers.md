---
category: Changed
---

- **MCP exposure now uses three settings** — `rewst-buddy.mcp.enable` exposes read capabilities, `rewst-buddy.mcp.enableWriteTools` exposes workflow writes, and `rewst-buddy.mcp.enableDangerousGraphqlMutation` separately exposes raw GraphQL mutation; the old MCP family checklist setting was removed.
