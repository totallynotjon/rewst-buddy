---
category: Added
pr: 80
---

- **MCP server (experimental): template write tools** — when write tools are enabled, external MCP clients can create, update, rename, and delete Rewst templates (`buddy_create_template`, `buddy_update_template_body`, `buddy_rename_template`, `buddy_delete_template`), each org-scoped and gated by a per-change approval inside VS Code.
