---
category: Fixed
---

- **Workflow tooling fixes** - workflow edits now prompt consistently, mock data must use the `mock_result` wrapper, workflow diagnose shows the executed path, and managed sub-org sessions resolve correctly.
