out
dist
node_modules
.vscode-test/
*.vsix
tsconfig.tsbuildinfo

# Local secrets — keep .env out, but track the template
.env
.env.local
!.env.example

# Workflow probe snapshots (may contain live workflow internals)
scripts/wf-backups/

# Local AI tool result artifacts (may contain live Rewst data)
.rewst-buddy/

# Claude Code local files
.claude/settings.*
.claudeignore
.LOCAL
